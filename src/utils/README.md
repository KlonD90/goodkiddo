# utils

Small shared helpers.

- `filesystem.ts` — read response formatting, line truncation, MIME detection helpers
- `semantic_numbers.ts` — number formatting used by execution result serialization
